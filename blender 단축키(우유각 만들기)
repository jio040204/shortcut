tab = 오브젝트 <-> 에딧 모드
1,2,3, = 점,선,면
G +(x,y,z,)= 움직이기+(x좌표로만, y좌표로만, z좌표로만)
E = 돌출시키기
alt + M = 합치기(점)
I = 안쪽으로 면 만들기(inset)
 //G =grab 잡다
 //E = extrude 돌출
 //R = rotate 회전
 //S = scale 크기조정
 //A = select all 전체선택
